# Optional local OCR reader. Install into the SAME environment as
# requirements.txt -- there is no separate .venv-easyocr any more.
#
#   python -m pip install -r requirements-easyocr.txt
#
# The app process still never imports Torch: easy_worker.py runs in a
# subprocess. CPU wheels are the tested portable default; for CUDA install the
# matching official PyTorch build first, then install this file.
easyocr==1.7.2
torch==2.14.0
torchvision==0.29.0
